// NOVO
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge",
      "request": "launch",
      "name": "Launch DEV Server URL",
      // Aponta para a URL correta do servidor
      "url": "http://localhost:3000/html/index.html",
      // Ajuda o VS Code a encontrar os arquivos fonte se precisar debugar
      "webRoot": "${workspaceFolder}/DEV"
    }
  ]
}
