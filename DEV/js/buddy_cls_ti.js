/*
 * Pilar 2: Manutenibilidade
 * Lógica específica para a página 'Etapa 5 - Buddy e CLS'.
 */
document.addEventListener('DOMContentLoaded', () => {
  // A lógica do 'scrollCarousel' foi removida pois o elemento
  // '#carousel' não existe neste HTML (Código Morto).

  // Lógica futura desta página (ex: interações com o card do Buddy)
  // virá aqui.
  console.log('Página Buddy/CLS carregada e script executado.');
});
